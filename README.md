# view-token
View large image of a token. Hover over the token and press the V key. To view the actor art for the token press Shift+V.

The name of the token/actor can be suppressed by checking the Hide Names setting.
